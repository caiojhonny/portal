{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "mock api to provide data",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/json-server --watch db.json --port=3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mock",
    "server",
    "mock",
    "data",
    "api"
  ],
  "author": "Caio Alcantara",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
